import React from "react";
import Counter from "./Counter";
import Hover from "./Hover";

const Hoc = () => {
  return (
    <div>
      <Counter />
      <Hover />
    </div>
  );
};

export default Hoc;
